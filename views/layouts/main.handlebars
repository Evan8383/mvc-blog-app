<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog App</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/css/styles.css">
</head>

<body class="bg-gray-100">
  <div class="container mx-auto px-4">
    <header class="py-4">
      <h1 class="text-2xl font-bold">
        <a href="/" class="text-blue-500 hover:text-blue-700">Home</a>
      </h1>
      <nav class="mt-4">
        {{#if loggedIn}}
        <a href="/dashboard" class="text-blue-500 hover:text-blue-700">dashboard</a>
        <button id="logout" class="btn-no-style logout ml-4">logout</button>
        {{else}}
        <a href="/login" class="text-blue-500 hover:text-blue-700">login</a>
        {{/if}}
      </nav>
    </header>
    <div class="site-row mt-8">
      <div class="site-name">
        <h1 class="text-3xl font-bold">Fancy Blog Application</h1>
      </div>
    </div>
    <main>
      {{{ body }}}
    </main>
  </div>

  {{#if loggedIn}}
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>